@{
    ViewBag.Title = "Редактор оружия";
}

<div class="col-12 my-2">@Html.ActionLink("Добавить новое", "EditWeapon", null, new { @class = "btn btn-success" })</div>

@Html.AjaxGrid(Url.Action("WeaponsGrid", "Editor"))

@using (Html.BeginForm("DeleteWeapon", "Editor", FormMethod.Post, new { id = "deleteForm" }))
{
    @Html.AntiForgeryToken();
    @Html.Hidden("ModelId")
}