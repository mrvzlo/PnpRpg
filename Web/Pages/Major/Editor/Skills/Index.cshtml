@page
@using Pnprpg.WebCore.Helpers
@model IndexModel
@{
    ViewData["Title"] = "Редактор навыков";
}

<div class="col-12 mb-3">
    <a class="btn btn-success" href="@Url.CustomPage(SitePages.MajorEditorSkillsEdit)">Добавить новый</a>
</div>

<div class="mvc-grid" data-source-url="@Url.CustomPage(SitePages.MajorEditorSkillsIndex, "Grid")"></div>

<form action="@Url.CustomPage(SitePages.MajorEditorSkillsIndex, "Delete")" method="post" asp-antiforgery="true" id="deleteForm">
    @Html.Hidden("ModelId")
</form>