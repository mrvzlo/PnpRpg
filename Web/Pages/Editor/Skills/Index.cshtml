@page
@model Pnprpg.WebCore.Pages.Editor.Skills.IndexModel
@{
    ViewData["Title"] = "Редактор навыков";
}

<div class="col-12 mb-3">
    <a class="btn btn-success" asp-page="@SitePages.EditorSkillsEdit">Добавить новый</a>
</div>

<div class="mvc-grid" data-source-url="@Url.Page(SitePages.EditorSkillsIndex, "Grid")"></div>

<form asp-page-handler="Delete" id="deleteForm">
    @Html.Hidden("ModelId")
</form>