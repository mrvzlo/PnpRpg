@page
@model Pnprpg.WebCore.Pages.Editor.Perks.IndexModel
@{
    ViewData["Title"] = "Редактор способностей";
}

<div class="col-12 mb-3">
    <a class="btn btn-success" asp-page="@SitePages.EditorPerksEdit">Добавить новую</a>
</div>

<div class="mvc-grid" data-source-url="@Url.Page(SitePages.EditorPerksIndex, "Grid")"></div>

<form asp-page-handler="Delete" id="deleteForm">
    @Html.Hidden("ModelId")
</form>