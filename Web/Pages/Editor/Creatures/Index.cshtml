@page
@model Pnprpg.WebCore.Pages.Editor.Creatures.IndexModel
@{
    ViewData["Title"] = "Редактор бестиария";
}

<div class="col-12 mb-3">
    <a class="btn btn-success" asp-page="@SitePages.EditorCreaturesEdit">Добавить новый</a>
</div>

<div class="mvc-grid" data-source-url="@Url.Page(SitePages.EditorCreaturesIndex, "Grid")"></div>

<form asp-page-handler="Delete" id="deleteForm">
    @Html.Hidden("ModelId")
</form>