@model Pnprpg.DomainService.Models.SpellViewModel
@{
    Layout = null;
}
<form method="get" asp-page="@SitePages.MagicIndex" asp-page-handler="Random" data-ajax="true"
      data-ajax-begin="AjaxJs.begin" data-ajax-success="AjaxJs.end" data-ajax-update="#randomSpell">
    <button class="btn btn-primary my-3" type="submit">
        Случайный свиток
    </button>
</form>
@if (!string.IsNullOrEmpty(Model.Name))
{
    <div class="h4 p-2 mw-260 d-inline-block border-colourful" style="--color: #@(Model.Color)">
        @Model.Name
    </div>
}