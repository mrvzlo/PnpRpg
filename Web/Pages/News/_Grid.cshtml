@using Microsoft.AspNetCore.Html
@using NonFactors.Mvc.Grid
@using Pnprpg.DomainService.Models
@using Pnprpg.WebCore.Helpers
@model IQueryable<Pnprpg.DomainService.Models.NewsViewModel>
@(Html.Grid(Model).Build(columns =>
{
    columns.Add(x => x).RenderedAs(Render).Titled("");
}).Css("table table-specific").Pageable(pager => { pager.RowsPerPage = 5; }))

@{
    IHtmlContent Render(NewsViewModel x) => PartialHelper.Body(
    @<div class="mb-4">@{await Html.RenderPartialAsync("_Details", x);}</div>
    );
}