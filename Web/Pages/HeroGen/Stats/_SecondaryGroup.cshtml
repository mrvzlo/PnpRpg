@using Pnprpg.DomainService.Enums
@model Pnprpg.DomainService.Models.HeroModel
@{
    await Html.RenderPartialAsync("Stats/SecondaryDetails", new SecondaryDetails
    {
        Color = Model.Abilities.Get((int) AbilityKey.E).Color, Value = Model.MaxHp(), Name = "Здоровье"
    });
    await Html.RenderPartialAsync("Stats/SecondaryDetails", new SecondaryDetails
    {
        Color = Model.Abilities.Get((int) AbilityKey.I).Color, Value = Model.MaxMp(), Name = "Запас маны"
    });
    await Html.RenderPartialAsync("Stats/SecondaryDetails", new SecondaryDetails
    {
        Color = Model.Abilities.Get((int) AbilityKey.E).Color, Value = Model.MaxAp(), Name = "Запас сил"
    });
    await Html.RenderPartialAsync("Stats/SecondaryDetails", new SecondaryDetails
    {
        Color = Model.Abilities.Get((int) AbilityKey.I).Color, Value = Model.Perception(), Name = "Восприятие"
    });

    await Html.RenderPartialAsync("Stats/SecondaryDetails", new SecondaryDetails
    {
        Color = Model.Abilities.Get((int) AbilityKey.S).Color, Value = Model.MaxCarry(), Name = "Вместимость"
    });
    await Html.RenderPartialAsync("Stats/SecondaryDetails", new SecondaryDetails
    {
        Color = Model.Abilities.Get((int) AbilityKey.S).Color, Value = Model.BaseDmg(), Name = "Урон"
    });
    await Html.RenderPartialAsync("Stats/SecondaryDetails", new SecondaryDetails
    {
        Color = Model.Abilities.Get((int) AbilityKey.E).Color, Value = Model.BaseArmor(), Name = "Защита"
    });
    await Html.RenderPartialAsync("Stats/SecondaryDetails", new SecondaryDetails()
    {
        Color = "828", Value = Model.Charisma, Name = "Харизма"
    });
}