@using Boot.Enums
@using Boot.Models
@model HeroModel
@{
    ViewBag.Title = "Home Page";
    var status = (Status)ViewBag.Status;
    switch (status)
    {
        case Status.Chaos: Html.RenderPartial("_Chaos"); break;
        case Status.Stats: Html.RenderPartial("_Stats", Model); break;
    }
}

@section scripts{
    <script>$(function () { MainJs.init(); });</script>
}
